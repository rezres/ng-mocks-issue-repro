import { OtherDirectiveDirective } from './other-directive.directive';

describe('OtherDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new OtherDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
